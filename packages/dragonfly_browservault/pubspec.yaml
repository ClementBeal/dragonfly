name: dragonfly_browservault
description: A starting point for Dart libraries or applications.
version: 0.0.1
# repository: https://github.com/my_org/my_repo

environment:
  sdk: ">=3.1.0 <4.0.0"

# Add regular dependencies here.
dependencies:
  sqlite3: ^2.4.6
  path: ^1.9.0

dev_dependencies:
  lints: ^4.0.0
  test: ^1.24.0
